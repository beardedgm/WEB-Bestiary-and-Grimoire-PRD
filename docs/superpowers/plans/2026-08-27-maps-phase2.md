# Maps (Phase 2) — full local HexPlora parity — 2026-08-27

**Status:** Shipped on `main` as of 2026-08-29. Implementation steps below are archival; see PRODUCT.md / specs for current behavior.


Supersedes `2026-08-27-maps-phase2-outline.md`. Interaction/tool parity spec: `docs/superpowers/specs/2026-08-28-maps-hexplora-parity.md`.

## Goal

Campaign **Maps** mode: many maps per active campaign; each = background image + hex overlay + fog + tokens + APP-class annotations; offline; **PixiJS**; IndexedDB `bg-maps`; HexPlora-compatible import/export.

## Architecture

- Meta: `bg.campaign.v1` → `campaign.maps[]` `{ id, name, updatedAt }`
- Heavy: IndexedDB DB `bg-maps` / store `maps` → `{ id, name, blob, state, updated }`
- UI: `#m-maps` + `#maps` map-first shell (left drawer + full-bleed stage); `window.MAPS` in `maps/maps-app.js`
- Vendor: `maps/maps-pixi.bundle.js` (Pixi 8.17.1 IIFE); rebuild via `maps/npm run build:pixi`
- Portable `bg-user-save/1`: maps **meta only**; map pixels via `.hexplora` export

## HexPlora parity matrix

Reference repo: `APP-Hexplora-PRD` (`client/src/input/keyboard.js`, `mouse.js`, `util/hitTest.js`, `pages/MapPage.jsx`).

| Area | HexPlora | B&G status |
| --- | --- | --- |
| Map-first layout | Vertical tools + left drawer; canvas hero | Done — `#mapsDrawer`, no right settings column |
| Pan selection | All object types | Done — `pickPanTarget` z-order: token → text → measurement → shape → stroke |
| Delete cascade | Keyboard + modal Delete | Done — `deleteSelection`, modal Delete buttons |
| Brush settings | Color, thickness, opacity, eraser | Done — drawer + `eraseAlongPath` |
| Shape settings | Stroke/fill; Shift/Alt modifiers | Done — drawer + `applyShapeModifiers`; shape sub-chips |
| Measure settings | Line color, distance/unit; labels; finalize UX | Done — `2026-08-30-maps-measure-parity.md` |
| Text settings | Color, size, outline; textarea | Done — drawer defaults on place; textarea editor |
| Token settings | Icon, color, size, multi-place, hex snap | Done — drawer; Alt free-place |
| Fog tools | Contextual when Reveal/Hide | Done — hint in drawer; grid/fog in Map section |
| Edit modals | Double-click stroke/shape/measurement | Done — `#mapsStrokeOvl`, `#mapsShapeOvl`, `#mapsMeasOvl` |
| Undo/Redo | Yes | Done |
| Import/Export `.hexplora` | Yes | Done |
| Screenshot PNG | Yes | Done |

## Deferred (non-goals)

Square grid UI, Library-linked tokens, Quest/Audio, share/QR, React HexPlora shell, map blobs in portable JSON, shape resize handles, shortcut help modal, Ctrl+D duplicate.

Follow-ups: [`2026-08-28-maps-phase3-deferred.md`](2026-08-28-maps-phase3-deferred.md). Full backlog: [`2026-08-28-backlog-roadmap.md`](2026-08-28-backlog-roadmap.md).
