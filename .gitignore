# Python
__pycache__/
*.py[cod]

# Scratch space from conversion runs. Directories under monsters/<system>/ and
# spells/<system>/ whose name starts with "_" are working files, not source
# books — the converters and build_bundles.py both skip them.
_to_delete/
convert.log
*.jsonl
*.jsonl.gz

# OS cruft
.DS_Store
Thumbs.db
